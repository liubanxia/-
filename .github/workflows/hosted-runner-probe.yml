name: Hosted Runner Probe

on:
  push:
    branches:
      - ci/hosted-runner-probe
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Probe
        run: |
          uname -a
          echo PUBLIC_RUNNER_OK
